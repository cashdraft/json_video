# Словарь anim (Later… / Remotion)

**Единственный источник:** [`data/later_anim_dictionary.json`](../data/later_anim_dictionary.json)

- Промт: `scenes_lab_later._build_later_response_contract()` подставляет `allowed` автоматически.
- Валидатор: `later_response_parse` → `later_anim_dictionary.allowed_anims_set()`.

Движок Remotion пока пустой. В `allowed` добавляйте имя **только после** реализации кубика в коде движка.

## Backlog (только NOTES, не anim)

См. поле `backlog` в JSON: parallax-drift, pulse-glow, twinkle, kerning-spread.
